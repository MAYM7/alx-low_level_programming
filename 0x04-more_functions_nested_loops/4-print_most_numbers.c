#include "main.h"

/**
 * print_numbers - a function that prints the numbers, from 0 to 9,
 * followed by a new line excipt 2 and 4.
 * Return: Always 0.
 */
void print_most_numbers(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		if (i = 2 && 4)
		{
			i++;
		}
		_putchar(i + '0');
	}
	_putchar ('\n');
}
