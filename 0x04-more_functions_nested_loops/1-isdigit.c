#include "main.h"

/**
 * _isdigit - a function that checks for a digit (0 through 9).
 * @c: the checked int.
 * Return: if c is through (0 to 9) 1 otherwise 0.
 */
int _isdigit(int c)
{
	return (c >= 0 && c <= 9);
}
