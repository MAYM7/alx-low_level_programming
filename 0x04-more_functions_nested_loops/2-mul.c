#include "main.h"

/**
 * mul - a function that multiplies two integers.
 * @a: first num to multiply.
 * @b: second num to multiply.
 * Return: always 0.
 */
int mul(int a, int b)
{
	return ('a' * 'b');
}
